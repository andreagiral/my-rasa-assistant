git rm --cached .env
git rm --cached -r models/
git commit -m "Remove .env and models from repo history"
git push

# Ignore virtual environment and Python cache
.venv/
__pycache__/
*.pyc

# Ignore model files and environment secrets
models/
.env

# VS Code and system files
.vscode/
.DS_Store
